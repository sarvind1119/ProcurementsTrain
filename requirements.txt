streamlit==1.8.0
PyPDF2==2.1.0
google-generativeai==0.1.0  # This is a placeholder; replace with the actual version if available
langchain==0.1.0  # This is a placeholder; replace with the actual version if available
python-dotenv==0.19.1
